{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for app windows",
  "windows": ["main", "mini"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
